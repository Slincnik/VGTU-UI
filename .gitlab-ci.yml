stages:
  - lint
  - build
  - release
  - deploy

variables:
  CONTAINER_RELEASE_IMAGE: $DOCKER_HUB_USER/cchgeu-frontend:$CI_COMMIT_TAG

default:
  image: node:20
  before_script:
    - corepack enable pnpm

cache:
  paths:
    - node_modules/
    - dist
    - deploy

include:
  - local: .gitlab/ci/*.gitlab-ci.yml
