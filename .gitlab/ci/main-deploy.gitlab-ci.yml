lint-job:
  stage: lint
  script:
    - pnpm install --frozen-lockfile
    - pnpm lint
  artifacts:
    paths:
      - node_modules/

build-job:
  stage: build
  needs:
    - lint-job
  script:
    - pnpm run build
  artifacts:
    paths:
      - dist/

workflow:
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH