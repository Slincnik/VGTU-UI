lint-job:
  stage: lint
  image: node:20
  before_script:
    - corepack enable pnpm
  script:
    - pnpm install --frozen-lockfile
    - pnpm lint
  artifacts:
    paths:
      - node_modules/

build-job:
  stage: build
  image: node:20
  needs:
    - lint-job
  before_script:
    - corepack enable pnpm
  script:
    - pnpm run build
  artifacts:
    paths:
      - dist/

workflow:
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH